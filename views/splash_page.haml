!!! 5
%html

  %head
    %meta(charset="utf-8")
    %title Welcome to Screenly

    %link(href="/static/css/bootstrap.css", rel="stylesheet")
    %link(href="/static/css/screenly.css", rel="stylesheet")

  %body.splash

    .container
      .row
        .span12
          .page-header.text-center
            %h1 Bievenue Dans le Systeme de Diffusion

      .row
        .span12.text-center
          - if ip_lookup:
            %p.lead Pour gérer le contenu de cet écran , il suffit de pointer votre navigateur sur:

            .well.lead
              %a(href="${url}") ${url}
          - else:
            %p.lead Incapable de résoudre la gestion IP.

      %hr

      .row
        .span12.text-center
          / Brought to you by <br />
          / %a(href="http://wireload.net")
          /   %img(src="/static/img/WireLoad.png")
